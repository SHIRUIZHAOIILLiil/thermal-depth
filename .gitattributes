# Windows 上 core.autocrlf 会把脚本检出成 CRLF，而 bash（WSL 和 Aire 上都是）
# 遇到 CRLF 会报 $'\r': command not found。这些文件强制 LF。
*.sh      text eol=lf
*.sbatch  text eol=lf
*.yaml    text eol=lf
*.yml     text eol=lf

# 二进制，不做行尾转换
*.png     binary
*.pt      binary
*.pth     binary
*.npy     binary
