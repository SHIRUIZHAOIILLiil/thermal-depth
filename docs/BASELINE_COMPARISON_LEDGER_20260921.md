# DA2 与 PPD：档案与当前状态（2026-09-21）

两个正在跑的对比对象。记它们是什么、怎么接进来的、现在卡在哪。

---

## 一、Depth Anything V2

| | |
|---|---|
| 出处 | NeurIPS 2024 |
| 权重 | `depth-anything/Depth-Anything-V2-Large-hf` |
| 结构 | DINOv2 编码器（ViT-L）+ DPT 解码器 |
| 参数 | 编码器 304.4 M + 解码器 30.9 M ＝ **335.3 M** |
| 原生输出 | 仿射不变**视差**（inverse depth） |
| 评估对齐 | `ssi_disparity` —— 它的原生空间，2 参数 |
| 预处理 | `AutoImageProcessor`：ImageNet 均值方差、518、保长宽比、边长取 14 的倍数 |

**零样本已测**（官方帧集待核）：

| 条件 | AbsRel | RMSE (m) | δ1 |
|---|---|---|---|
| day | 0.1529 | 7.1309 | 0.8114 |
| night | 0.1652 | 6.9056 | 0.7768 |
| rainy | 0.1720 | 7.1681 | 0.7542 |

⚠️ **赢零样本 DA2 不算数。** 它从没见过热像，这证明的是「热像适配有用」，
是这个领域的前提不是我们的贡献。所以才要有下面那条适配线。

### 我们的适配线

**路径**：`tools/finetune_depth_anything_thermal.py`

他们的仓库只放出**米制**变体的微调代码，而米制是仿射不变表里的错目标，所以配方
是我们自己写的。这反而对公平性更好 —— 监督可以做到和我们自己那条线**逐项相同**：

- 官方 8 序列划分
- 补全伪深度 + 真实激光覆写(同一份目标)
- 视差空间的 `masked_ssi_l1`(同一个损失)
- 同一条 val 选点规则

⭐ **编码器不冻结。** 他们自己的米制配方就是用解码器十分之一的学习率微调编码器；
冻住它等于让一个 RGB 预训练的编码器盯着热像看 —— 赢一个被绑住一只手的对手证明不了
任何事。编码器 `5e-6`，解码器 `5e-5`，20000 步，batch 4。

**门禁**：第 0 步的 AbsRel 必须落在 `[0.08, 0.30]`。它挡下过两个 bug ——
没走 `DPTImageProcessor`，以及诊断参照拿了补全伪深度而不是激光。

⚠️ 每 2000 步 `save_pretrained`，但**没有 resume 逻辑**。被时限杀掉的话
checkpoint 都在，接不上，只能重来或者用手上最好的那个。

### 选点规则（2026-09-22 定，跑 test 之前）

⭐ **val AbsRel 最低；并列时取更早的那一步** → **step16000**。

官方 val 三序列全量（14,249 帧，`ssi_disparity`）：

| checkpoint | AbsRel | RMSE (m) | δ1 |
|---|---|---|---|
| step2000 | 0.08662 | 3.989 | 0.91283 |
| step4000 | 0.09204 | 3.957 | 0.90682 |
| step6000 | 0.09112 | 4.493 | 0.90263 |
| step8000 | 0.08350 | 3.610 | 0.92264 |
| step10000 | 0.09085 | 3.931 | 0.91199 |
| step12000 | 0.08456 | 3.915 | 0.91806 |
| step14000 | 0.10386 | 5.752 | 0.88091 |
| **step16000** | **0.08078** | **3.484** | **0.92980** |
| step18000 | 0.08135 | 3.502 | 0.92909 |
| step20000 | 0.10389 | 4.191 | 0.88905 |

⚠️⚠️ **这条线的训练不稳，而且那不是采样噪声。** 相邻点跳动中位 0.00735；
40 帧的冒烟上是 0.01575。帧数涨了 356 倍，纯采样该降 18.9 倍（→ 0.00083），
实测只降 2.1 倍 —— **约九成是 checkpoint 自己在抖，加帧数不会变。**

所以：
- ⛔ **step16000 与 step18000 分不出**（差 0.00057，抖动的十三分之一）
- ⭐ 但 `{16000, 18000}` 这一组明显好于其余八点（差 0.02 以上，远超抖动）
- **规则处理的是这个并列**，取更早的一步；这条规则与数据无关，所以看不看 test 都一样

⭐ **报 DA2 的数时，表注必须写上 val 的离散程度** —— 最差点 0.104 比最好点 0.081
差 28%。只报 0.081 会让那个数看着比它实际稳固。

---

## 二、Pixel-Perfect Depth

| | |
|---|---|
| 出处 | NeurIPS 2025 |
| 参数 | 约 **500 M** |
| 结构 | **像素空间扩散，没有 VAE** —— DiT，depth 24、hidden 1024、patch 8（有效 16） |
| 训练目标 | **log 深度，2%/98% 分位** |
| 原生输出 | **未声明** —— 论文说目标是 log 深度，`run.py` 又对输出做了 min-max 上色，两边都没说 `infer_image` 返回什么 |
| 评估对齐 | **待定**，用 `--probe-output-space` 在训练帧上实测 |

### ⭐ 为什么它不只是一个对比对象

**1. 它就是「去掉 VAE」的那条路。** 我们现在的瓶颈测量全在输出侧的 VAE 往返上；
PPD 在像素空间直接扩散，根本没有这个环节。它的成绩是那条路值不值得走的现成证据 ——
**比我们自己动手换解码器便宜得多**。

**2. 它的目标就是我们刚换上的那个。** log 深度 2%/98% 分位。这是当初指向 log 的
三条独立证据之一(另外两条是我们自己的 VAE 往返实测，和 Marigold V2 的默认选择)。

### 我们的集成

**路径**：`integrations/ppd/`

| 文件 | 干什么 |
|---|---|
| `ms2_thermal.py` | 数据集，拷进克隆树当 `ppd/data/ms2_thermal.py` |
| `prepare_ms2.py` | 造 split JSON + 转 checkpoint |
| `train_ms2_thermal.yaml` | 训练配置，batch 4，AdamW `1e-4` |

放在我们仓库而不是他们的:改了什么留在自己的审查下,而且他们的克隆树永远是没被我们
编辑过的。

数据集只覆写三个方法(其余基类已经做了):

- `read_rgb` —— 他们读 8 位彩色除以 255;我们是 16 位单通道热像,拉伸到 8 位用
  **1%/99% 裁剪**(min-max 会让中位帧损失 14.3 点的相邻差分辨率),和我们自己那条线一致
- `read_depth` —— 补全伪深度 + 激光覆写,同一份目标
- `build_metas` —— 从我们的 manifest 生成他们那种两条并行路径列表

### ⚠️ 已经踩掉的一个静默坑

他们仓库里**两个加载器互相打架**:

```
load_pretrained_model        torch.load(path)["state_dict"]              训练用
load_pretrained_model_eval   torch.load(path)，dit.* -> pipeline.dit.*   推理用
```

发布的 `ppd.pth` 是第二种形式,而训练走 `find_last_ckpt_path`,它**只 glob `*.ckpt`**
然后直接索引 `["state_dict"]`。所以拿已发布权重微调会从三个方向失败 —— 扩展名不对、
键不存在、名字没前缀 —— **而且找不到就静默从随机初始化开始训**,没有任何报错。

`prepare_ms2.py` 照搬他们 eval 加载器的重映射,再包一层训练加载器要的外壳。

---

## 二点五、AnyThermal 放在哪(2026-09-21 补)

**和 DA2 同族**:前馈判别式 + 相对深度(MiDaS 系),按 `ssi` 对齐,不是扩散模型。
但 **AnyThermal 是热像原生的,DA2 没见过热像** —— 所以这一族里 AnyThermal 是硬
对手,DA2 零样本是弱的,那条适配线存在的理由就是把弱的换成硬的。

数已经齐了(官方子集,三条件三指标):AbsRel 0.0810 / 0.0872 / 0.1020,
RMSE 2.614 / 2.514 / 3.159,δ1 0.9424 / 0.9380 / 0.9048。**不用再跑。**

⚠️ **但它生成我们的训练目标**(`build_anythermal_pseudo_gt.py`:它的预测逐帧对
激光标定,再当作另一个 GT 来源)。赢它说明的是**激光覆写加训练补进了它没有的
东西**,不说明架构更好。⭐ **这一句必须进表注**,审稿人翻到伪 GT 来源就会问。
措辞按项目规矩走「监督部分来自 AnyThermal 的预测」,禁用 teacher / distill。

✅ **已核(2026-09-21):不是零样本。** AnyThermal 的深度监督就是
**MS2 自己的 train split + 稀疏 LiDAR GT**,和我方同源(`AIRE_RESULTS_20260802.md`
§复现验证,出处是它论文 §III-B / §III-F / §VI-C:"depth baselines are trained on
the evaluation datasets")。复现核对:它 Table IV 报 0.0883 / 2.7432(三条件平均),
我方重跑 0.0821(仅 day),量级吻合。

⛔ **坑**:它 §III-B 有一句 "MS² reserved for zero-shot evaluation" —— **那句只管
VPR,不管深度**。只看那句会把它误判成零样本。

⭐ 所以这个对比**在数据上是公平的**,和 DA2 零样本不是一回事,赢它有含义。剩下的
差异要进表注:骨干 DINOv2 ViT-B/14 vs SD U-Net;深度头 MiDaS vs 扩散单步 x0;
**训练时骨干冻结只训头 vs 整个 U-Net 解冻**。最后一条可能不只是差异而是解释 ——
稀疏激光在远端与天空无信号,全解冻的 U-Net 在无监督区漂移,冻结骨干保住了 DINOv2
的语义先验,这对得上我方 RMSE 全输而 AbsRel/δ1 全赢的形状。

---

## 三、现在在等什么

| 作业 | ID | 申请 | 预计开始 | 节点 | 出来之后 |
|---|---|---|---|---|---|
| `ceiling` | 7927600 | 40 分 | **今晚 19:51** | gpu010 | log 下的 VAE 天花板 + 同批帧的模型成绩 |
| `ppd_smoke` | 7927066 | 30 分 | **今晚 19:41** | gpu023 | 确认 PPD 真的从已发布权重起步，不是随机初始化 |
| `da2ft` | 7927065 | 4 小时 | 9-23 14:50（估） | gpu008 | DA2 适配后的三条件数 |

集群本身是好的(22 mix + 4 alloc,只有 gpu021 drain、gpu028 down),是被别人占满了。
`squeue --start` 的时间按「前面每个人都用满时限」估,实际可能提前。

### 先看哪个

**`ceiling`。** 它决定另外两个的意义:如果模型已经贴上 log 的新天花板,那「换
condition / 跟外部模型比」这一整条支线都在同一条线下面争同一点空间。反过来才值得
往 DA2 和 PPD 上继续投。

---

## 四、附:三处待核(顺手发现的,与上面无关)

写这份台账时发现三份文档在「基线几参数」上打架,记下来免得忘:

1. `COMPARISON_PROTOCOL_20260826.md` §1 说 DORN/BTS/AdaBins/NeWCRF 已发表数字是
   **中位数缩放 1 参数**;`EXPERIMENT_LADDER_20260902.md` §5.2 说「五个基线全部是
   深度空间**两参数**」并引用了前者;同文 L264 又说「给基线 2 参数比它们发表的
   1 参数更强(NeWCRF day RMSE **2.717 → 2.430**)」。最可能是 §5.2 那张表是我方
   重跑的强化版,措辞说成了基线自己的协议。**未证实。**
2. `RESULTS_20260921_LOG_LINE.md` §5.2 拿我方 **1 参数**米制头对 NeWCRF 的
   `0.0716/2.430/0.9556` —— 而那正是 §5.2 那张 **2 参数**表里的值。若第 1 条成立,
   这个「三项全输」是在比我们更不利的口径下得出的。
3. ~~基线的 RMSE 与 δ1 只有 day~~ —— **写错了,已更正(2026-09-21)**。
   `EXPERIMENT_LADDER_20260902.md` 四点九节有完整横向表(2026-09-05):DORN /
   AnyThermal / AdaBins / BTS / NeWCRF **三条件 × 三指标全有**,官方子集、逐图
   2 参数、各在原生空间。"只有 day3"那条限制是**我方 `r3ow_tn_cap` 那条臂**的,
   不是基线的。所以主表不缺基线的格子,缺的是**把 log 线填进去**——拼表,不是算力。

⚠️ `COMPARISON_PROTOCOL_20260826.md` 的**协议部分仍然有效**,但它里面的**我方数字
已作废**(那是 `trunc_disparity` 时代的,隔了两次目标参数化)。
